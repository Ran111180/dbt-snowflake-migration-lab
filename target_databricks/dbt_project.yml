name: dbt_migration_lab
version: '1.0.0'
config-version: 2
profile: dbt_migration_lab
model-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
clean-targets: ["target", "dbt_packages"]
models:
  dbt_migration_lab:
    staging: {+schema: bronze, +materialized: view}
    intermediate: {+schema: silver, +materialized: table}
    marts: {+schema: gold, +materialized: table}
    advanced: {+schema: silver, +materialized: table}
vars:
  lookback_days: 30
  scd_valid_to: '9999-12-31'
  los_short_stay: 7
  los_long_stay: 60
seeds:
  dbt_migration_lab: {+schema: seeds}
